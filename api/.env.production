# =============================================================================
# GRIFO API - PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================
# This file contains production-ready environment variables
# Make sure to update with your actual production values

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Environment
NODE_ENV=production

# Server port
PORT=5000

# API version
API_VERSION=v1

# Application name
APP_NAME="Grifo API - Production"

# Application URL (update with your production domain)
APP_URL=https://your-production-domain.com

# =============================================================================
# SECURITY SETTINGS
# =============================================================================

# JWT Secret (MUST be changed to a secure random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_IN_PRODUCTION

# JWT expiration time
JWT_EXPIRES_IN=24h

# JWT refresh token expiration
JWT_REFRESH_EXPIRES_IN=7d

# Bcrypt rounds for password hashing
BCRYPT_ROUNDS=12

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================

# Supabase project URL (update with your production Supabase URL)
SUPABASE_URL=https://your-production-project.supabase.co

# Supabase anonymous key (update with your production anon key)
SUPABASE_ANON_KEY=your-production-supabase-anon-key

# Supabase service role key (update with your production service role key)
SUPABASE_SERVICE_ROLE_KEY=your-production-supabase-service-role-key

# =============================================================================
# CORS SETTINGS
# =============================================================================

# Allowed origins for CORS (update with your production domains)
CORS_ORIGIN=https://your-production-portal.com,https://your-production-api.com

# Allow credentials in CORS
CORS_CREDENTIALS=true

# =============================================================================
# RATE LIMITING
# =============================================================================

# Rate limit window in milliseconds (15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window (stricter for production)
RATE_LIMIT_MAX_REQUESTS=50

# Rate limit message
RATE_LIMIT_MESSAGE="Too many requests, please try again later."

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level (warn for production to reduce noise)
LOG_LEVEL=warn

# Log format (json for production)
LOG_FORMAT=json

# Log file path
LOG_FILE=logs/production.log

# Maximum log file size in MB
LOG_MAX_SIZE=50

# Maximum number of log files to keep
LOG_MAX_FILES=10

# =============================================================================
# FILE UPLOAD SETTINGS
# =============================================================================

# Maximum file size in bytes (5MB for production)
MAX_FILE_SIZE=5242880

# Maximum number of files per request
MAX_FILES_PER_REQUEST=5

# Allowed file types
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp,application/pdf

# Upload directory
UPLOAD_DIR=uploads

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================

# SMTP settings (update with your production email service)
SMTP_HOST=smtp.your-email-provider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-production-email@your-domain.com
SMTP_PASS=your-production-email-password

# Email sender information
EMAIL_FROM_NAME="Sistema Grifo"
EMAIL_FROM_ADDRESS=noreply@your-domain.com

# =============================================================================
# EXTERNAL SERVICES (Optional - Configure as needed)
# =============================================================================

# Google Drive Configuration (if using)
# GOOGLE_DRIVE_CLIENT_ID=your-production-google-drive-client-id
# GOOGLE_DRIVE_CLIENT_SECRET=your-production-google-drive-client-secret
# GOOGLE_DRIVE_REDIRECT_URI=https://your-production-domain.com/api/auth/google/callback
# GOOGLE_DRIVE_SCOPES=https://www.googleapis.com/auth/drive.file
# GOOGLE_DRIVE_FOLDER_NAME=Grifo_Vistorias_Producao

# OneDrive Configuration (if using)
# ONEDRIVE_CLIENT_ID=your-production-onedrive-client-id
# ONEDRIVE_CLIENT_SECRET=your-production-onedrive-client-secret
# ONEDRIVE_REDIRECT_URI=https://your-production-domain.com/api/onedrive/callback
# ONEDRIVE_SCOPES=Files.ReadWrite,User.Read
# ONEDRIVE_FOLDER_NAME=Grifo_Vistorias_Producao

# =============================================================================
# MONITORING (Recommended for production)
# =============================================================================

# Sentry DSN for error tracking (highly recommended)
# SENTRY_DSN=https://your-production-sentry-dsn@sentry.io/project-id

# =============================================================================
# IMPORTANT PRODUCTION NOTES
# =============================================================================
# 1. Change all placeholder values to actual production values
# 2. Generate a secure JWT_SECRET using crypto.randomBytes(64).toString('hex')
# 3. Use your actual Supabase production project credentials
# 4. Update CORS_ORIGIN with your actual production domains
# 5. Configure proper email service for notifications
# 6. Set up error monitoring with Sentry or similar service
# 7. Never commit this file to version control
# 8. Ensure all secrets are properly secured